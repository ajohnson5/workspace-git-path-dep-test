[project]
name = "c"
dynamic=["version"]
dependencies = [
    "d"
]
requires-python = ">=3.11"

[tool.uv.sources]
d = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "../../__about__.py"
